meta {
  name: Health Check
  type: http
  seq: 1
}

get {
  url: http://localhost:8000/
  body: none
  auth: none
}

tests {
  test("Status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });

  test("API is running", function() {
    var data = res.getBody();
    expect(data.status).to.equal("ok");
  });
}
